void setup();{
  size(displayWidth,displayHeight);
  background(250,250,250);
}

void draw();{
  if (mousePressed) {
     stroke(0,0,0);
     fill(0,0,0);
     ellipse(mouseX, mouseY, 10, 10);
  } 

 
}
